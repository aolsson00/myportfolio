# R packages

This directory is reserved for **R package** source (e.g. `DESCRIPTION`, `R/`, `man/`) when you publish or coursework requires it.

Right now the folder does not contain an actual package—only this README. Add your package tree here when you have one, then describe the package name, purpose, and how to install with `devtools::install()` or `remotes::install_github()`.
